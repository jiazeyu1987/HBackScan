# GET `/`

- 入口：`code/hospital_scanner/main.py:99`
- 作用：返回服务简单自描述，包含硬编码版本与运行状态。

## 执行流程
1. FastAPI 路由匹配后直接返回字典常量，不依赖数据库或后台组件。

## 设计原因
- 作为最简“存活探针”与欢迎页，让浏览器/负载均衡能快速确认应用是否启动。

## 优点
- 无外部依赖，响应极快。
- 结构极简，几乎没有失败面。

## 缺点与风险
- 版本号与状态为硬编码，无法反映真实部署版本或后端健康度。
- 未附带任何诊断信息，无法辅助排障。

## 改进建议
- 从单一配置源读取版本（如 `pyproject` 或 git 标签），避免手工同步。
- 可附带启动时间、构建哈希或健康检查链接，提升可观测性。
